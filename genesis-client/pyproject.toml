[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "genesis-client"
version = "0.1.0"
description = "Python SDK for Nano Spiking Engine"
dependencies = [
    "toml",
]
