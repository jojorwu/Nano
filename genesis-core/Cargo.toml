[package]
name = "genesis-core"
version = "0.1.0"
edition = "2021"

[features]
default = ["titan", "text", "vision"]
titan = []
text = []
vision = []

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
bytemuck = { version = "1.13", features = ["derive"] }
bincode = "1.3"
